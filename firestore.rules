rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents to anyone for now.
    // This is not secure for a production app, but fine for development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
